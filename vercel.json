{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "dist/**",
        "outputDirectory": "dist"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "api/index.js" }],
  "buildCommand": "npm run build"
}
